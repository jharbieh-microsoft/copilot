import { spInvokableFactory, _SPInstance } from "../spqueryable.js";
export class _SitePageService extends _SPInstance {
    constructor(baseUrl, path = "_api/SP.Publishing.SitePageService") {
        super(baseUrl, path);
    }
    /**
    * Gets current user unified group memberships
    */
    getCurrentUserMemberships() {
        const q = SitePageService(this, null);
        q.concat(".GetCurrentUserMemberships");
        return q();
    }
}
export const SitePageService = spInvokableFactory(_SitePageService);
//# sourceMappingURL=types.js.map